import Layout from "@/app/components/Layout";

const Settings = () => {
    return (
        <Layout>
            <h1 className="title">Settings</h1>
        </Layout>
    );
}

export default Settings;